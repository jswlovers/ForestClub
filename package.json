{
  "name": "forest-club",
  "version": "1.0.0",
  "private": true,
  "description": "포레스트클럽 — 골프·여행 멤버십 서비스 (백엔드 + 정적 프론트엔드)",
  "type": "commonjs",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "make-admin": "node --env-file-if-exists=.env scripts/make-admin.js"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.19.2",
    "multer": "^2.4.0"
  }
}
